@using Ext.Net.MVC;

@(
 Html.X().Window().Height(560).Width(950)
    .Title("پیش نمایش")
    .AutoScroll(true)
    .Closable(true)
    .Modal(true)
    .CloseAction(Ext.Net.CloseAction.Destroy)
                        .Html("<center><object class='se-pre-con2' style='width: 900px;height: 540px;border: 1px solid #ccc;' type='application/pdf' data='/NewVer/Filtering_New/RptMafasa?MafasaId=" + ViewBag.MafasaId + "'></object></center>")

)
