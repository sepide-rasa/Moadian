@*@using Ext.Net.MVC;

@(
 Html.X().Window().Height(560)
 .Width(1100)
    .Title("پیش نمایش")
    .AutoScroll(true)
    .Closable(true)
    .CloseAction(Ext.Net.CloseAction.Destroy)
               .Html("<center><object class='se-pre-con2' style='width: 1000px;height: 540px;border: 1px solid #ccc;' type='application/pdf' data='/NewVer/Filtering_New/RptResid?CollId=" + ViewBag.CollId + "'></object></center>")

)*@
@using Ext.Net.MVC;

@(
 Html.X().Window().Height(560).Width(900)
        .Title("پیش نمایش")
        .AutoScroll(true)
        .Layout(Ext.Net.LayoutType.Fit)
        .Closable(true)
        .Border(false)
        .CloseAction(Ext.Net.CloseAction.Destroy)
        .Modal(true)
    .Html("<center><object class='se-pre-con2' style='width: 800px;height: 540px;border: 1px solid #ccc;' type='application/pdf' data='/NewVer/SelectParvande/RptResid?id=" + @ViewBag.Id + "&Type=" + @ViewBag.Type + "'></object></center>")
)